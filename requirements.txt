# =============================================================================
# Requirements for running Qwen2-VL CHAIR Benchmark on NVIDIA RTX 6000
# Supports: NVIDIA RTX 6000 Ada Generation, RTX A6000, Quadro RTX 6000
# Recommended Python: 3.10 or 3.11
# =============================================================================
#
# Quick setup command for CUDA 12.1 on RTX 6000:
#   pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# =============================================================================

# Deep Learning Framework (CUDA-enabled)
torch>=2.2.0
torchvision>=0.17.0

# HuggingFace & Qwen2-VL Support
transformers>=4.45.2
accelerate>=0.28.0
qwen-vl-utils>=0.0.8

# CHAIR Benchmark NLP & Evaluation Dependencies
nltk>=3.8.1

# Core Utilities & Vision
pillow>=10.0.0
pyyaml>=6.0
tqdm>=4.66.0
numpy>=1.24.0,<2.0.0
sentencepiece>=0.1.99

# =============================================================================
# Optional: FlashAttention-2 for RTX 6000 (Ada Generation / RTX A6000)
# To install FlashAttention-2 (requires CUDA toolkit):
#   pip install flash-attn --no-build-isolation
# =============================================================================